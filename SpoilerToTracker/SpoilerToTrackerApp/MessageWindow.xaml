<Window x:Class="SpoilerToTrackerApp.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message Window"
        Height="450" Width="800"
        ResizeMode="CanResize"
        MinHeight="450"
        MinWidth="550"
        MaxHeight="450"
        MaxWidth="550"
        WindowStartupLocation="CenterOwner"
        Background="#FF212121"
        Foreground="White">

    <Grid Margin="10">
        <!-- Define layout rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Message -->
            <RowDefinition Height="Auto"/>
            <!-- Message -->
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Button -->
        </Grid.RowDefinitions>

        <!-- TOP: Message -->
        <TextBlock Grid.Row="0"
                   Text="Conversion Complete!"
                   TextAlignment="Center"
                   Width="550"
                   FontSize="16"
                   Background="#FF8B5CF6"
                   Foreground="White"
                   
                   Margin="0,0,0,10"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap" />

        <!-- Middle: Message -->
        <TextBlock Grid.Row="2"
           Text="You will need to assign the following dungeon rewards still:"
           FontSize="14"
           Foreground="White"
           Margin="0,0,0,10"
           HorizontalAlignment="Center"
           TextWrapping="Wrap" />

        <!-- MIDDLE: DataGrid -->
        <DataGrid x:Name="SpoilerDataGrid"
                  Grid.Row="3"
                  Background="#FF212121"
                  Foreground="White"
                  BorderBrush="#FF5E5E5E"
                  HeadersVisibility="Column"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#FF5E5E5E"
                  VerticalGridLinesBrush="#FF5E5E5E"
                  AlternationCount="2"
                  Margin="0,0,0,10">

            <!-- Row Styling -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                    <Setter Property="Background" Value="#FF212121"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Style.Triggers>
                        <Trigger Property="AlternationIndex" Value="1">
                            <Setter Property="Background" Value="#FF2E2E2E"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF8B5CF6"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#FF8B5CF6"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="#FF9B7CF8"/>
                        </MultiTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <!-- Header Styling -->
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#FF212121"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="#FF5E5E5E"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="Padding" Value="4,2"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <!-- Cell Styling -->
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Foreground"
                                    Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Foreground}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Complete Dungeon" Binding="{Binding Dungeon}" />
                <DataGridTextColumn Header="Reward" Binding="{Binding Reward}" />
                <DataGridTextColumn Header="Note" Binding="{Binding Note}" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="4"
               Text="To view this list again:"
               FontSize="14"
               FontWeight="Bold"
               TextDecorations="Underline"
               Foreground="White"
               Margin="0,0,0,5"
               HorizontalAlignment="Center"
               TextWrapping="Wrap" />

        <TextBlock Grid.Row="5"
           Text="View Spoiler Details -> Category -> Dungeon Rewards"
           FontSize="14"
           Foreground="White"
           Margin="0,0,0,20"
           HorizontalAlignment="Center"
           TextWrapping="Wrap" />

        <!-- BOTTOM: Button -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center">
            
            <Button Content="Close"
                    Width="300"
                    Margin="0,0,0,0"
                    Padding="5,2"
                    Background="#FF444444"
                    Foreground="White"
                    BorderBrush="#FF5E5E5E"
                    Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
